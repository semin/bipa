<% content_for :main_title do %>
  <div class="title">
    <%= @scop.hierarchy %>: <%= @scop.description %>
  </div>
<% end %>

<div id="tab_menu_container">
  <ul id="tab_menu">
    <li><%= link_to "Hierarchy", hierarchy_scop_path(@scop), :class => "current" %></li>
    <li><%= link_to "Distributions", distributions_scop_path(@scop) %></li>
    <li><%= link_to "Propensites", propensities_scop_path(@scop) %></li>
    <li><%= link_to "Chi-square Test", chisquare_test_scop_path(@scop) %></li>
    <li><%= link_to "Alignments", scop_alignments_path(@scop) %></li>
    <li><%= link_to "Interfaces", scop_interfaces_path(@scop) %></li>
  </ul>
</div>

<div id="below_tab_menu">
  <% if @scop.ancestors.size > 0 %>
    <%= render :partial => "/scops/relative", :collection => @scop.ancestors.send("rep#{session[:redundancy]}").send("res#{session[:resolution]}") %>
  <% end %>

  <div class="current_hierarchy">
    <% @scop.level.times do %>
      &nbsp;&nbsp; 
    <% end %>

    &#45; <b><%= @scop.hierarchy %></b>: <%= link_to "#{@scop.description}", hierarchy_scop_path(@scop) %>
  </div>

  <% if @scop.level < 4 && @scop.children.size > 0 %>
    <%= render :partial => "/scops/relative", :collection => @scop.children.send("rep#{session[:redundancy]}").send("res#{session[:resolution]}") %>
  <% end %>

  <% if @scop.siblings.size > 0 %>
    <%= render :partial => "/scops/relative", :collection => @scop.siblings.send("rep#{session[:redundancy]}").send("res#{session[:resolution]}") %>
  <% end %>
</div>
